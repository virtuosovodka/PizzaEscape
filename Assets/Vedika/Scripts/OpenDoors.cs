//using System.Collections;
//using System.Collections.Generic;
using UnityEngine;

public class OpenDoors : MonoBehaviour
{
    // Start is called before the first frame update
    /*
     * dough bags in order, fridge door open takes you automatically into kitchen
     * kitchen door opens when garlic knots are torched and able to push button
     * basement door opens when picture "scanned" on scanner
     * when piano played, a, b, x and y can take you around to rooms, fade to black and then open
     */
    
    /*
     void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
